# Git
.git
.gitignore

# IDEs and editors
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Python virtual environments
venv/
env/
*.venv/
pip-wheel-metadata/

# Python bytecode and caches
__pycache__/
*.pyc
*.pyo
*.pyd

# Python build artifacts and test caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
build/
dist/
*.egg-info/
htmlcov/
.coverage
coverage.xml

# Secrets - these should be passed as environment variables or Docker secrets
.env
config/secrets.env
api_keys_setup_instructions.md 
# Consider if config.yaml contains secrets; if so, it might need special handling or exclusion

# Large data directories not needed in the final image
# Adjust these based on what your production application actually needs.
# If models or specific data files ARE needed, they should be copied explicitly
# in the Dockerfile, or this .dockerignore should be more granular.
# data/ 
# dataset/
# results/
# output/ # Typically contains generated files, models might be here.
          # If models from 'output/enhanced' are needed, don't ignore all of 'output/'
          # or COPY them specifically.

# Logs - container logs should go to stdout/stderr
logs/

# Documentation and notebooks
docs/
notebooks/
*.ipynb
mkdocs.yml

# Test files (if not running tests in the final production image)
tests/
pytest.ini

# OS-specific files
.DS_Store
Thumbs.db

# Docker files themselves
Dockerfile
Dockerfile.*
.dockerignore

# Other development/utility scripts or files not needed for runtime
scripts/temp_get_signal_id.py 
# scripts/ # Be careful if some scripts ARE needed for runtime (e.g., entrypoint helpers)

# Zip files or other archives unless specifically needed
*.zip
*.tar.gz

# Specific files
v2.txt
temp.json
tf_deps.txt
trading_deps.txt
trading_env_deps.txt
